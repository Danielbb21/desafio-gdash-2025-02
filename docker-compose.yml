services:
  # backend:
  #   build: ./backend
  #   container_name: backend
  #   ports:
  #     - "3000:3000"
  #   env_file:
  #     - ./backend/.env

  colector:
    build: ./colector
    container_name: colector
    restart: always
